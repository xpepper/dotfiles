alias ldots='ls -a | egrep "^\.[^\.]"'

alias studio='cd ~/Documents/studio'

# Git
alias gitdiff='git diff -w | mate'
alias gb='git branch'
alias gc='git commit -v'
alias gca='git commit -a -v'
alias gco="git checkout"
alias gd='git diff'
alias gdc='git diff --cached' # What's added but not yet committed?
alias gdh='git diff HEAD'
alias gp='git push'
alias gl='git pull'
alias gpr='git pull --rebase'
alias gst='git status'
alias gap='git add -p'
alias glod='git log --oneline --decorate'
alias gl="git log --stat"

alias workspace='cd ~/Documents/workspace/'

# Rails
alias sc="[ -f script/console ] && script/console || bundle exec rails console"
alias ss="[ -f script/server ] && script/server || rails server"
alias sdbc='script/dbconsole'

# for EPI
alias epi='cd ~/Documents/workspace/epistore/epistore'
alias tsp='cd ~/Documents/workspace/tuttosport'
alias sss='script/ec2/ssh-instances'
alias rakelite='rake $* 2>/dev/null'
alias me='script/mate.sh'

alias hosts='sudo vim /etc/hosts'

#for Mikamai
alias jbb='cd ~/Documents/workspace/job-board; rvm use 1.9.3-p125@job-board; rvm wrapper 1.9.3-p125@job-board textmate'

# MongoDB
# alias mongostart='mongod run --config /usr/local/etc/mongod.conf'
